/*
 * Entry de JavaScript do projeto.
 *
 * Ainda vazio de propósito: o layout continua carregando Bootstrap e o
 * SweetAlert2 por CDN, e os arquivos de public/js/ pela diretiva @js. A fase 2
 * do QA-06 traz tudo para cá, junto com os três globais que o JS inline das
 * views ainda lê do window (Drinkerito, Swal, bootstrap).
 * Ver docs/superpowers/specs/2026-09-16-migracao-vite-design.md
 *
 * O arquivo precisa existir e buildar já agora, senão a fase 1 não consegue
 * provar que o build roda no Railway.
 */
